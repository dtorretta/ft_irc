1)
necesito que en el header de channel agregues como metodo publico:

void broadcast(const std::string& message, Client* exclude = nullptr);


y en el .cpp de channel:

void broadcast(const std::string& message, Client* exclude = nullptr)
    {
        for (std::vector<Client>::iterator it = clients.begin(); it != clients.end(); ++it)
        {
            if (&(*it) != exclude)
                server->_sendResponse(message, it->get_fd());
        }

        for (std::vector<Client>::iterator it = admins.begin(); it != admins.end(); ++it)
        {
            if (&(*it) != exclude)
                server->_sendResponse(message, it->get_fd());
        }
    }
NOTA: Listo!

2)
en en server.cpp estoy usando dentro de la funcion get_channelByName un metodo de channel
std::string GetName();
NOTA: Listo!

si le cambias el nombre, tambien habria que cambiarlo ahi (por ahora es la linea 334)

3)
en RegistrationCommands.cpp estoy usando la funcion GetClientInChannel que e sun un metodo de channel
si le cambias el nombre, tambien habria que cambiarlo ahi (por ahora es la linea 66)
NOTA: se llama isClientInChannel (ya lo cambie en registration commands), poro retorna un bool.
	  creo que en tu function tienes que colocar: if (It->isClientInChannel(oldNickname))
