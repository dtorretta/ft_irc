/*
Clase Client

Representa a un cliente conectado.

Contiene:
    Descriptor de socket.
    Estado del cliente (nick, username, si está autenticado, canales a los que pertenece).
    Buffer de lectura y escritura para manejar parcialidad en TCP.

Métodos:
    Lectura/Escritura parcial.
    Envío de mensajes (raw).
    Actualización de estado.

*/

#pragma once

class Client
{

};
